# Copy to .env.local. Not NEXT_PUBLIC_*: this is read by the Next server inside
# the /api/* proxy route, never by the browser, so it stays a runtime value
# rather than being inlined into the client bundle at build time.
BACKEND_URL="http://localhost:3001"
