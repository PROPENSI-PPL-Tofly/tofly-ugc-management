"use client";

export interface SubmitDraftModalProps {
  content: {
    id: string;
    name: string;
    deadline: string;
  };
  onClose: () => void;
}

export function SubmitDraftModal(
  _props: SubmitDraftModalProps,
) {
  return null;
}